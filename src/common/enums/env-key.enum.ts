export enum EnvKey {
  DbHost = 'DB_HOST',
  DbPort = 'DB_PORT',
  DbUser = 'DB_USER',
  DbPassword = 'DB_PASSWORD',
  DbName = 'DB_NAME',
  JwtSecret = 'JWT_SECRET',
  JwtExpiresIn = 'JWT_EXPIRES_IN',
}
